#include<iostream>
using namespace std;
class rectangle
{
    float length;
	float width;
public:


   void inputDimensions()
    {
        cout << " enter length, width";
        cin >>length>>width;
    }

	float calculateArea() // getter
    {
        
        return (length+);
    }
	float calculatePerimeter()
 // getter
    {
        cout << "\n name;" << name << "\n marks2 :" << marks2 << "\nmarks3:" << marks3;
        cout << "\nmarks1:" << marks1 << "\n marks2:" << "\nmarks3:" << marks3;
        cout << "\ntotal:" << calculatetotal();
    }
};
int main()
{
    student s1;
    s1.inputdetails();
    s1.displaydetails();
    return 0;
}